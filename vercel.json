{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "next build",
  "installCommand": "corepack enable && pnpm install --no-frozen-lockfile",
  "env": {
  },
  "crons": [
    {
      "path": "/api/news?refresh=1",
      "schedule": "0 */6 * * *"
    }
  ]
}
